const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2)); // se não colocar parâmetro, imprime o corpo da função

const multiplicarPorDois2 = x => x * 2;

console.log(multiplicarPorDois2(4))