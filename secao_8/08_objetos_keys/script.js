let obj = {
    'chave1': 1, //chave = propriedade, 1 = valor da propriedade
    'chave2': 2,
    'chave3': 3
}

console.log(obj);

console.log(Object.keys(obj)); //retorna um array, facilitando o trabalho